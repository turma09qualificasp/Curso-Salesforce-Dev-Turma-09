public class PropertyBuilder extends AbstractPropertyBuilder{

    public PropertyBuilder(Id municipioId, String cep, Decimal area, Decimal length, Decimal price) {
        super(municipioId, cep, area, length, price);
    }
}